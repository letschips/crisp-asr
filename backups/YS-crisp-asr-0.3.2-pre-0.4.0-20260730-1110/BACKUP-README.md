# YS Crisp ASR 0.3.2 rollback snapshot

Created before deploying Crisp ASR 0.4.0 to the YS vault on 2026-07-30.

The `runtime` folder preserves the exact YS plugin runtime and its vault-local
settings at that point:

- `main.js`
- `manifest.json`
- `styles.css`
- `data.json`

This is a private rollback snapshot. Do not include it in public release ZIPs.
